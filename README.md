# cva-pipeline-hull-white-nn
CVA pipeline pricing an interest rate swap under a Hull-White short-rate model, with a neural network trained as a fast surrogate pricer. Computes Expected Exposure, 95% PFE, and CVA via Monte Carlo, validated against the analytical benchmark (R² = 0.999).
